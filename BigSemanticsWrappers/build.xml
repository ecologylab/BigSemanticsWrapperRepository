<?xml version="1.0" encoding="UTF-8"?>

<project name="BigSemanticsWrappers" basedir="." default="main">
	<property name="repo.dir" value="MmdRepository" />
	<property name="build.dir" value="build" />
	<property name="classes.dir" value="${build.dir}/classes" />
	<property name="jar.dir" value="${build.dir}/jar" />
	<property name="jar.file" value="${jar.dir}/${ant.project.name}.jar" />
	<property name="jar.file.in-sdk" value="../../BigSemanticsJava/BigSemanticsSDK/lib/${ant.project.name}.jar" />
	
  <target name="clean">
    <delete dir="${build.dir}" />
  </target>
 
	<target name="compile">
		<mkdir dir="${classes.dir}" />
		<copy todir="${classes.dir}">
		  <fileset dir="${repo.dir}" />
		</copy>
	</target>
		
	<target name="jar" depends="compile">
	  <mkdir dir="${jar.dir}" />
		<jar destfile="${jar.file}" basedir="${classes.dir}" />
		<copy file="${jar.file}" tofile="${jar.file.in-sdk}" />
	</target>
	
	<target name="main" depends="clean,jar" />
	
</project>
